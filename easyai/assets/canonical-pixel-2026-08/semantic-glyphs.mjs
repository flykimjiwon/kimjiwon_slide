const join = (parts) => parts.flat(Infinity).filter(Boolean).join("");

const box = (r, x, y, u, color, width = 9, height = 8) => r(x, y, u * width, u * height, color);
const stepped = (r, x, y, u, color, rows) => join(rows.map(([col, row, width, height = 1]) => r(x + col * u, y + row * u, width * u, height * u, color)));

export function glyphForName(ctx) {
  const { name, x, y, u, c, paint, hash, r, frame, core, check, danger, ghost, document, server } = ctx;
  const accent = paint[hash(name) % paint.length];
  switch (name) {
    case "window": return join([frame(x, y, u * 11, u * 8, c.cyan), r(x + u, y + u * 2, u * 9, u, c.white), r(x + u * 5, y + u * 2, u, u * 5, c.line), r(x + u * 2, y + u * 4, u * 2, u * 2, c.sky), r(x + u * 7, y + u * 4, u * 2, u * 2, c.blue)]);
    case "tokens": return join([frame(x, y + u, u * 10, u * 7, c.cyan), [2, 3.5, 5, 6.5].map((row, index) => r(x + u * (2 + (index % 2)), y + u * row, u * (6 - (index % 2)), u, paint[index]))]);
    case "bag": return join([box(r, x, y + u * 2, u, c.yellow, 10, 7), r(x + u * 3, y, u * 4, u * 3, c.yellow), r(x + u * 4, y + u, u * 2, u, c.canvas), r(x + u * 4, y + u * 5, u * 2, u * 2, c.white)]);
    case "compass": return join([stepped(r, x, y, u, c.white, [[4, 0, 2], [2, 2, 6], [0, 4, 10], [2, 6, 6], [4, 8, 2]]), r(x + u * 4, y + u * 3, u * 2, u * 4, c.cyan), r(x + u * 3, y + u * 4, u * 4, u * 2, c.yellow)]);
    case "cloud": return join([stepped(r, x, y + u, u, c.pink, [[3, 0, 4], [1, 2, 8], [0, 4, 10], [1, 6, 8], [3, 8, 4]]), r(x + u * 4, y + u * 4, u * 2, u, c.white)]);
    case "anchor": return join([r(x + u * 4, y, u * 2, u * 7, c.blue), r(x + u * 2, y + u * 2, u * 6, u, c.white), r(x, y + u * 6, u * 2, u * 2, c.blue), r(x + u * 8, y + u * 6, u * 2, u * 2, c.blue), r(x + u * 2, y + u * 8, u * 6, u, c.blue)]);
    case "plan": return join([document(x, y, u, c.yellow), check(x + u * 3, y + u * 5, u / 2)]);
    case "gear": return join([r(x + u * 3, y, u * 4, u * 10, accent), r(x, y + u * 3, u * 10, u * 4, accent), r(x + u * 2, y + u * 2, u * 6, u * 6, c.white), r(x + u * 4, y + u * 4, u * 2, u * 2, c.deep)]);
    case "observe": return join([stepped(r, x, y + u * 2, u, c.white, [[3, 0, 4], [1, 2, 8], [0, 4, 10], [1, 6, 8], [3, 8, 4]]), r(x + u * 3, y + u * 3, u * 4, u * 4, c.cyan), r(x + u * 4, y + u * 4, u * 2, u * 2, c.ink)]);
    case "map": return join([frame(x, y, u * 11, u * 8, c.green), r(x + u * 3, y, u, u * 8, c.line), r(x + u * 7, y, u, u * 8, c.line), r(x + u * 2, y + u * 5, u * 7, u, c.yellow), r(x + u * 7, y + u * 3, u, u * 3, c.yellow)]);
    case "switchboard": return join([frame(x, y, u * 11, u * 8, c.blue), [2, 5, 8].map((col, index) => [r(x + u * col, y + u * 2, u, u * 4, c.white), r(x + u * col, y + u * (2 + index), u, u, paint[index])])]);
    case "plug": return join([r(x + u * 2, y + u * 2, u * 6, u * 5, accent), r(x + u * 3, y, u, u * 3, c.white), r(x + u * 6, y, u, u * 3, c.white), r(x + u * 4, y + u * 7, u * 2, u * 3, c.cyan)]);
    case "key": return join([r(x, y + u * 4, u * 8, u * 2, c.yellow), r(x + u * 7, y + u * 3, u * 3, u * 4, c.yellow), r(x + u * 2, y + u * 6, u, u * 2, c.yellow), r(x + u * 4, y + u * 6, u, u * 3, c.yellow), r(x + u * 8, y + u * 4, u, u * 2, c.canvas)]);
    case "port": return join([frame(x, y, u * 10, u * 8, c.blue), r(x + u * 2, y + u * 3, u * 6, u * 2, c.ink), r(x + u * 3, y + u * 3, u, u, c.cyan), r(x + u * 6, y + u * 3, u, u, c.cyan)]);
    case "rail": return join([r(x + u, y, u, u * 9, c.yellow), r(x + u * 8, y, u, u * 9, c.yellow), r(x + u, y + u * 4, u * 8, u, c.white), check(x + u * 4, y + u * 5, u / 2)]);
    case "filter": return join([r(x, y, u * 10, u * 2, c.yellow), r(x + u, y + u * 2, u * 8, u * 2, c.yellow), r(x + u * 3, y + u * 4, u * 4, u * 3, c.yellow), r(x + u * 4, y + u * 7, u * 2, u * 3, c.cyan)]);
    case "baton": return join([r(x, y + u * 3, u * 10, u * 3, c.yellow), r(x, y + u * 2, u * 2, u * 5, c.cyan), r(x + u * 8, y + u * 2, u * 2, u * 5, c.cyan), r(x + u * 4, y + u * 4, u * 2, u, c.white)]);
    case "dispatcher": return join([r(x + u, y + u * 3, u * 9, u * 5, c.blue), r(x + u * 2, y + u, u * 7, u * 3, c.yellow), r(x + u * 2, y + u * 5, u * 2, u, c.white), r(x + u * 7, y + u * 5, u * 2, u, c.white)]);
    case "shelf": return join([r(x, y, u * 10, u, c.yellow), r(x, y + u * 4, u * 10, u, c.yellow), r(x, y + u * 8, u * 10, u, c.yellow), [1, 4, 7].map((col, index) => r(x + u * col, y + u * (index % 2 ? 1 : 5), u * 2, u * 3, paint[index]))]);
    case "lens": return join([stepped(r, x, y, u, c.cyan, [[3, 0, 4], [1, 2, 8], [1, 4, 8], [3, 6, 4]]), r(x + u * 3, y + u * 2, u * 4, u * 4, c.white), r(x + u * 6, y + u * 6, u * 4, u * 2, c.yellow), r(x + u * 8, y + u * 7, u * 2, u * 2, c.yellow)]);
    case "pin": return join([r(x + u * 3, y, u * 4, u * 4, c.pink), r(x + u * 2, y + u * 2, u * 6, u * 3, c.pink), r(x + u * 4, y + u * 5, u * 2, u * 4, c.white), r(x + u * 4, y + u * 9, u * 2, u, c.cyan)]);
    case "noise": return join([[0, 3, 7].map((col, index) => r(x + u * col, y + u * (index % 2 ? 5 : 2), u * 2, u * 2, c.coral)), r(x + u * 4, y, u * 2, u * 2, c.pink), r(x + u * 4, y + u * 7, u * 2, u * 2, c.coral)]);
    case "funnel": return join([r(x, y, u * 10, u * 2, c.cyan), r(x + u, y + u * 2, u * 8, u * 2, c.cyan), r(x + u * 3, y + u * 4, u * 4, u * 2, c.cyan), r(x + u * 4, y + u * 6, u * 2, u * 4, c.yellow)]);
    case "press": return join([r(x, y, u * 10, u * 2, c.yellow), r(x, y + u * 8, u * 10, u * 2, c.yellow), r(x + u * 3, y + u * 3, u * 4, u * 4, c.white), r(x + u * 4, y + u * 2, u * 2, u, c.coral), r(x + u * 4, y + u * 7, u * 2, u, c.coral)]);
    case "cube": return join([r(x + u * 2, y, u * 6, u * 3, c.sky), r(x, y + u * 3, u * 6, u * 6, c.blue), r(x + u * 6, y + u * 3, u * 4, u * 6, c.deep), r(x + u * 3, y + u * 4, u * 2, u * 2, c.white)]);
    case "toolwall": return join([frame(x, y, u * 10, u * 9, c.cyan), r(x + u * 2, y + u * 2, u, u * 5, c.yellow), r(x + u * 4, y + u * 3, u * 4, u, c.white), r(x + u * 6, y + u * 2, u, u * 5, c.pink)]);
    case "archive": return join([box(r, x, y + u * 2, u, c.blue, 10, 6), r(x + u, y, u * 8, u * 3, c.sky), r(x + u * 3, y + u * 4, u * 4, u, c.white)]);
    case "power": return join([frame(x, y, u * 9, u * 9, c.blue), stepped(r, x + u * 2, y + u, u, c.yellow, [[3, 0, 2], [2, 2, 3], [4, 4, 2], [1, 6, 3]])]);
    case "stack": return join([0, 3, 6].map((row, index) => [r(x + u * (index % 2), y + u * row, u * 8, u * 2, paint[index]), r(x + u * (index % 2), y + u * (row + 2), u * 6, u, c.white)]));
    case "drawers": return join([frame(x, y, u * 10, u * 9, c.yellow), [1, 4, 7].map((row) => [r(x + u, y + u * row, u * 8, u * 2, c.blue), r(x + u * 4, y + u * row, u * 2, u, c.white)])]);
    case "bench": return join([r(x + u, y + u * 3, u * 9, u * 2, c.yellow), r(x + u * 2, y + u * 5, u, u * 4, c.blue), r(x + u * 8, y + u * 5, u, u * 4, c.blue), r(x + u * 3, y + u, u * 2, u * 2, c.green), r(x + u * 6, y + u, u * 2, u * 2, c.pink)]);
    case "bridge": return join([r(x, y + u * 4, u * 10, u * 2, c.cyan), r(x + u, y + u * 6, u, u * 3, c.blue), r(x + u * 8, y + u * 6, u, u * 3, c.blue), r(x + u * 3, y + u * 2, u * 4, u * 2, c.white)]);
    case "elevator": return join([frame(x + u * 2, y, u * 6, u * 10, c.blue), r(x + u * 3, y + u * 3, u * 4, u * 4, c.sky), r(x + u * 4, y + u, u * 2, u, c.white), r(x + u * 4, y + u * 8, u * 2, u, c.white)]);
    case "card": return join([box(r, x, y, u, c.white, 8, 10), r(x, y, u * 8, u, c.pink), r(x + u * 2, y + u * 3, u * 2, u * 2, c.sky), r(x + u * 5, y + u * 3, u * 2, u, c.line), r(x + u * 5, y + u * 5, u * 2, u, c.line)]);
    case "directory": return join([frame(x, y, u * 10, u * 9, c.green), [2, 4, 6].map((row, index) => [r(x + u, y + u * row, u * 2, u, paint[index]), r(x + u * 4, y + u * row, u * 4, u, c.white)])]);
    case "badge": return join([stepped(r, x, y, u, c.yellow, [[3, 0, 4], [1, 2, 8], [2, 6, 6], [3, 8, 4]]), r(x + u * 3, y + u * 3, u * 4, u * 2, c.white)]);
    case "gate": return join([r(x + u, y, u, u * 9, c.yellow), r(x + u * 8, y, u, u * 9, c.yellow), r(x + u, y + u * 2, u * 8, u * 2, c.cyan), r(x + u * 4, y + u * 4, u * 2, u * 3, c.white)]);
    case "checkpoint": return join([r(x + u, y + u * 2, u * 5, u * 5, c.blue), r(x + u * 2, y, u * 3, u * 2, c.yellow), r(x + u * 6, y + u * 5, u * 4, u, c.yellow), r(x + u * 8, y + u * 4, u, u * 3, c.yellow)]);
    case "bins": return join([0, 3.5, 7].map((col, index) => [r(x + u * col, y + u * 2, u * 3, u * 6, paint[index]), r(x + u * col, y + u, u * 3, u, c.white)]));
    case "mold": return join([frame(x, y, u * 10, u * 9, c.cyan), r(x + u * 3, y + u * 2, u * 4, u * 5, c.ink), r(x + u * 4, y + u * 3, u * 2, u * 3, c.white)]);
    case "wrench": return join([r(x + u * 3, y + u * 3, u * 5, u * 2, c.yellow), r(x + u * 2, y + u * 5, u * 2, u * 4, c.yellow), r(x + u * 7, y + u, u * 3, u * 3, c.yellow), r(x + u * 8, y + u, u, u, c.canvas)]);
    case "spiral": return join([r(x, y, u * 9, u, c.cyan), r(x, y, u, u * 9, c.cyan), r(x, y + u * 8, u * 7, u, c.cyan), r(x + u * 6, y + u * 3, u, u * 6, c.cyan), r(x + u * 3, y + u * 3, u * 4, u, c.yellow), r(x + u * 3, y + u * 3, u, u * 4, c.yellow)]);
    case "pipe": return join([r(x, y + u * 3, u * 7, u * 3, c.cyan), r(x + u * 6, y + u * 3, u * 3, u * 7, c.cyan), r(x + u, y + u * 4, u * 5, u, c.white)]);
    case "packets": return join([0, 3, 6].map((col, index) => [r(x + u * col, y + u * (index % 2 ? 4 : 2), u * 2, u * 3, paint[index]), r(x + u * col, y + u * (index % 2 ? 5 : 3), u * 2, u, c.white)]));
    case "train": return join([0, 3.5, 7].map((col, index) => [r(x + u * col, y + u * 2, u * 3, u * 4, paint[index]), r(x + u * col, y + u * 6, u, u, c.white), r(x + u * (col + 2), y + u * 6, u, u, c.white)]));
    case "route": return join([r(x + u * 4, y, u * 2, u * 9, c.cyan), r(x + u * 4, y + u * 4, u * 5, u * 2, c.cyan), r(x, y + u * 4, u * 5, u * 2, c.cyan), r(x + u * 7, y + u * 3, u * 2, u * 4, c.yellow)]);
    case "orders": return join([0, 2, 4].map((row, index) => [r(x + u * (index % 2), y + u * row, u * 8, u * 3, c.white), r(x + u * (index % 2), y + u * row, u * 8, u, c.pink), r(x + u * (2 + index), y + u * (row + 2), u * 3, u / 2, c.line)]));
    case "org": return join([r(x + u * 4, y, u * 2, u * 2, c.yellow), r(x + u * 4, y + u * 2, u * 2, u * 2, c.cyan), r(x + u, y + u * 4, u * 8, u, c.cyan), [0, 4, 8].map((col) => r(x + u * col, y + u * 6, u * 2, u * 2, c.blue))]);
    case "turnstile": return join([r(x + u * 4, y + u * 3, u * 2, u * 5, c.yellow), r(x, y + u * 4, u * 10, u * 2, c.yellow), r(x + u * 2, y + u * 2, u * 2, u * 2, c.cyan), r(x + u * 6, y + u * 6, u * 2, u * 2, c.cyan)]);
    case "stamp": return join([r(x + u * 3, y, u * 4, u * 3, c.yellow), r(x + u * 2, y + u * 3, u * 6, u * 2, c.yellow), r(x + u, y + u * 6, u * 8, u * 3, c.blue), r(x + u * 3, y + u * 7, u * 4, u, c.white)]);
    case "lab": return join([r(x + u * 4, y, u * 2, u * 5, c.white), stepped(r, x, y + u * 4, u, c.green, [[3, 0, 4], [1, 2, 8], [0, 4, 10], [1, 6, 8]]), r(x + u * 4, y + u * 6, u * 2, u, c.white)]);
    case "scanner": return join([r(x + u, y + u * 2, u, u * 7, c.yellow), r(x + u * 8, y + u * 2, u, u * 7, c.yellow), r(x + u, y + u * 2, u * 8, u, c.yellow), r(x + u * 2, y + u * 5, u * 6, u, c.cyan)]);
    case "trace": return join([0, 2, 4, 6, 8].map((col, index) => r(x + u * col, y + u * (index % 2 ? 5 : 2), u, u, paint[index % paint.length])));
    case "replay": return join([frame(x, y, u * 10, u * 8, c.blue), r(x + u * 2, y + u * 3, u * 5, u * 2, c.white), r(x + u * 2, y + u * 2, u * 2, u * 4, c.cyan), r(x + u * 7, y + u * 2, u * 2, u * 4, c.cyan)]);
    case "aquarium": return join([frame(x, y, u * 10, u * 9, c.cyan), r(x + u, y + u * 5, u * 8, u * 3, c.blue), r(x + u * 3, y + u * 3, u * 2, u * 2, c.white), r(x + u * 7, y + u * 2, u, u, c.sky)]);
    case "pen": return join([0, 3, 6, 9].map((col) => r(x + u * col, y, u, u * 9, c.yellow)).concat([r(x, y + u * 3, u * 10, u, c.yellow), r(x, y + u * 7, u * 10, u, c.yellow)]));
    case "obstacle": return join([1, 4, 7].map((col, index) => [r(x + u * col, y + u * 2, u * 2, u, paint[index]), r(x + u * col, y + u * 3, u, u * 5, c.white), r(x + u * (col + 1), y + u * 3, u, u * 5, c.white)]));
    case "tiles": return join([[0, 0], [5, 0], [0, 5], [5, 5]].map(([col, row], index) => r(x + u * col, y + u * row, u * 4, u * 4, paint[index])));
    case "vector": return join([frame(x, y, u * 10, u * 9, c.blue), [2, 4, 6].map((row) => r(x + u, y + u * row, u * 8, u / 2, c.line)), [2, 4, 6].map((col) => r(x + u * col, y + u, u / 2, u * 7, c.line)), r(x + u * 2, y + u * 6, u * 5, u, c.cyan), r(x + u * 6, y + u * 3, u, u * 4, c.cyan)]);
    case "switch": return join([r(x, y + u * 4, u * 10, u * 2, c.cyan), r(x + u * 4, y + u * 2, u * 2, u * 6, c.cyan), r(x + u * 6, y + u * 2, u * 3, u * 2, c.yellow)]);
    case "postal": return join([0, 4, 8].map((col, index) => [r(x + u * col, y + u * 2, u * 3, u * 5, c.blue), r(x + u * col, y + u * 2, u * 3, u, paint[index])]));
    case "pantry": return join([r(x, y, u * 10, u, c.yellow), r(x, y + u * 5, u * 10, u, c.yellow), [1, 4, 7].map((col, index) => r(x + u * col, y + u * (index % 2 ? 1 : 6), u * 2, u * 3, paint[index]))]);
    case "coat": return join([r(x + u * 4, y, u * 2, u * 2, c.white), r(x + u * 3, y + u * 2, u * 4, u, c.white), stepped(r, x, y + u * 3, u, c.blue, [[3, 0, 4], [1, 2, 8], [0, 4, 10], [1, 6, 8]])]);
    case "seed": return join([r(x + u * 4, y + u * 5, u * 2, u * 4, c.yellow), r(x + u, y + u * 2, u * 4, u * 3, c.green), r(x + u * 5, y, u * 4, u * 3, c.green), r(x + u * 4, y + u * 7, u * 2, u * 2, c.white)]);
    case "mail": return join([box(r, x, y + u * 2, u, c.white, 10, 6), r(x + u, y + u * 3, u * 4, u, c.pink), r(x + u * 5, y + u * 3, u * 4, u, c.pink), r(x + u * 4, y + u * 4, u * 2, u * 2, c.pink)]);
    case "campfire": return join([r(x + u, y + u * 7, u * 8, u * 2, c.yellow), r(x + u * 2, y + u * 6, u * 6, u, c.coral), stepped(r, x + u * 2, y, u, c.pink, [[2, 0, 2], [1, 2, 4], [0, 4, 6]])]);
    case "board": return join([frame(x, y, u * 10, u * 8, c.white), r(x + u * 2, y + u * 2, u * 2, u * 2, c.yellow), r(x + u * 6, y + u * 2, u * 2, u * 2, c.pink), r(x + u * 4, y + u * 5, u * 3, u, c.cyan), r(x + u * 2, y + u * 8, u, u * 2, c.blue), r(x + u * 7, y + u * 8, u, u * 2, c.blue)]);
    case "bucket": return join([r(x + u * 2, y + u * 3, u * 6, u * 6, c.blue), r(x + u, y + u * 2, u * 8, u * 2, c.sky), r(x + u * 3, y, u * 4, u * 2, c.white), r(x + u * 4, y + u * 5, u * 2, u, c.white)]);
    case "screen": return join([frame(x, y, u * 10, u * 8, c.yellow), r(x + u * 2, y + u * 2, u * 6, u * 3, c.blue), r(x + u * 4, y + u * 5, u * 2, u * 3, c.white), r(x + u * 2, y + u * 8, u * 6, u, c.white)]);
    default: return null;
  }
}
